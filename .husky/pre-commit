#!/usr/bin/env sh

command -v gitleaks >/dev/null 2>&1 || {
  echo "gitleaks is required for pre-commit secret scanning."
  exit 1
}

pnpm exec lint-staged || exit 1
pnpm run secrets:staged || exit 1
